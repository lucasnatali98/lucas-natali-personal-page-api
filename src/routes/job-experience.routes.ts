import { Router, Request, Response } from "express";

const jobExperienceRoutes = Router();

jobExperienceRoutes.get("/", async (req: Request, res: Response) => {});
jobExperienceRoutes.post("/", async (req: Request, res: Response) => {});

export { jobExperienceRoutes };
